server:
  port: 8084
  forward-headers-strategy: framework

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finflow_document?createDatabaseIfNotExist=true
    username: root
    password: Durga@123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

rabbitmq:
  exchange: loan.exchange
  routing:
    key:
      uploaded: document.uploaded.routing.key

springdoc:
  api-docs:
    path: /api/documents/v3/api-docs
